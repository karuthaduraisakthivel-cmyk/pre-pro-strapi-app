'use strict';

/**
 * publication-viewer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::publication-viewer.publication-viewer');
