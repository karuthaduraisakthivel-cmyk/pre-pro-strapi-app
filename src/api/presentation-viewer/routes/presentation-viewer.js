'use strict';

/**
 * presentation-viewer router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::presentation-viewer.presentation-viewer');
